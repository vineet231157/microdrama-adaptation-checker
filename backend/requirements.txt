# ── Web / API ──────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# ── Task queue + state ───────────────────────────────────────────────────────
celery==5.4.0
redis==5.2.1

# ── Google Drive ─────────────────────────────────────────────────────────────
google-api-python-client==2.157.0
google-auth==2.37.0
google-auth-httplib2==0.2.0

# ── AI / Gemini ──────────────────────────────────────────────────────────────
google-generativeai==0.8.3
tenacity==9.0.0
# Pin grpc/protobuf so pip doesn't backtrack for hours resolving them.
protobuf==4.25.5
grpcio==1.68.1
grpcio-status==1.68.1

# ── Subtitle / OCR (Step 1) ──────────────────────────────────────────────────
# Subtitle→SRT extraction is done in app/pipeline/subtitle_ocr.py (PaddleOCR +
# OpenCV) — NO external videocr dependency. paddlepaddle is CPU by default;
# swap for paddlepaddle-gpu on a CUDA host.
paddlepaddle==2.6.2
paddleocr>=2.6,<3.0.0
numpy<2
pysrt==1.1.2
rapidfuzz==3.11.0

# ── Documents / PDF ──────────────────────────────────────────────────────────
reportlab==4.2.5
pdfplumber==0.11.5
python-docx==1.1.2
