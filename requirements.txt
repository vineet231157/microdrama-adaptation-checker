# ─────────────────────────────────────────────────────────────────────────────
# LIGHT deps — installs cleanly on Streamlit Community Cloud (no GPU/OCR stack).
# Covers: the UI, Formatting-Only, screenplay generation, evaluation, Drive, PDFs.
#
# The heavy subtitle-OCR stack (PaddleOCR/videocr/OpenCV) is in
# requirements-ocr.txt and is installed ONLY in the Docker image, because it is
# large and needs a real server/GPU. Step 1 (OCR) therefore runs on the Docker
# deployment, not on Community Cloud.
# ─────────────────────────────────────────────────────────────────────────────

# ── Streamlit UI ─────────────────────────────────────────────────────────────
streamlit==1.40.2

# ── AI / Gemini ──────────────────────────────────────────────────────────────
google-generativeai==0.8.3
tenacity==9.0.0

# ── Google Drive (service-account mode) ──────────────────────────────────────
google-api-python-client==2.157.0
google-auth==2.37.0

# ── Documents / PDF ──────────────────────────────────────────────────────────
reportlab==4.2.5
pdfplumber==0.11.5
python-docx==1.1.2
pysrt==1.1.2

# state.py imports redis at module load (functions are monkeypatched in the
# Streamlit path, but the import must resolve). Pure-Python, light.
redis==5.2.1
